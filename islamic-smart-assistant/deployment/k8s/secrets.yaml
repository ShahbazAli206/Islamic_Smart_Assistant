# Replace placeholder values via `kubectl create secret`, kustomize secretGenerator,
# or an external secrets controller (External Secrets Operator + AWS Secrets Manager / Vault).
apiVersion: v1
kind: Secret
metadata:
  name: backend-env
stringData:
  DATABASE_URL: "postgresql://USER:PASS@db-host:5432/islamic_assistant"
  REDIS_URL: "redis://redis-host:6379"
  JWT_PRIVATE_KEY: ""
  JWT_PUBLIC_KEY: ""
  S3_ACCESS_KEY: ""
  S3_SECRET_KEY: ""
  FCM_SERVICE_ACCOUNT_JSON: ""
